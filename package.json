{
  "name": "anime-tracker",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "cd client && npm ci && VITE_API_URL=/api npm run build && mkdir -p ../server/static && cp -r dist/* ../server/static/"
  }
}
