{
  "name": "anime-tracker",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "powershell -ExecutionPolicy Bypass -File scripts/dev.ps1",
    "heroku-postbuild": "cd client && npm ci --include=dev && VITE_API_URL=/api npm run build && mkdir -p ../server/static && cp -r dist/* ../server/static/"
  }
}
